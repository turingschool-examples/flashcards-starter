const expect = require('chai').expect;
var Turns = require('../src/Turns');

describe('Turn', function() {
  it.skip('should have a guess' function() {
    let turn = new Turn(userGuess);
    expect(turn).to.equal(userGuess)
  })




})
