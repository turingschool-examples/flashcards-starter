class Deck {
  constructor(cards) {
    this.cards = cards || [];
    this.count = 0;
  }  

  countCards() {
    //count number of cards instances in the array
    //increment this.count
    //return the this.count number
  }
}