// function createDeck(cards) {
//   deck = [cards]
//   return deck
// }

const createDeck = (cards) => {
  // console.log(cards)
  let deck = [cards]
  console.log(deck)
  return deck
}

module.exports = {
  createDeck
}