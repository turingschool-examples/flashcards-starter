class Deck {
    constructor(cards) {
        this.cards = cards,
        this.cardCount = 
    }

    countCards = () => {
        let count = this.cards.length;
        return count
    }
}

module.exports = Deck