// being called from Game


class Deck {
  constructor(array) {
    //forEach(element) instantiate Card?
    //where will I create a card?
  }
  countCards() {
    //return deck.length
  }

}

module.exports = Deck;
