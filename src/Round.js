class Round {
  constructor(deck) {
    this.deck = deck ? deck.cards : [];
    this.turnCount = 0;
    // this.currentCard = deck.cards[0];
  }

  returnCurrentCard() {
  //   for (var i = 0; i < this.deck.length; i++) {
  //     console.log("heloooo", deck.length)
  //     return deck[i];
  //   }
  }
  // this.currentCard = this.currentCard 
}

module.exports = Round;
