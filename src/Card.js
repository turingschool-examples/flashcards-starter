class Card{
  constructor(card, question, answers, correctAnswer){
    this.card = card
    this.question = question
    this.answers = answers
    this.correctAnswer = correctAnswer
  }
}

module.exports = Card
